% Calculates the forward pass for the entire network
% Stores the values of signals and outputs from each layer
% Calls forwardPass for each layer in the network

% Inputs:

% Outputs:

function Y = forwardPassNetwork()


end